#!/usr/bin/env bash
# Keep one implementation beside the tmux configuration.
set -euo pipefail
script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$script_dir/../tmux/scripts/lazy.py" "$@"
