#!/usr/bin/env bash

set -euo pipefail

# Preserve the existing current-session scope, skipping unloaded panes.
for window in $(tmux list-panes -s -F '#{?@lazy_cwd,,#{pane_id}}'); do
  tmux send-keys -t "$window" "$@" C-m
done

