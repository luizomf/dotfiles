#!/usr/bin/env bash

set -euo pipefail

# Pending panes have no process to receive input; never wake them implicitly.
for window in $(tmux list-panes -a -F '#{?@lazy_cwd,,#{pane_id}}'); do
  tmux send-keys -t "$window" "$@" C-m
done

