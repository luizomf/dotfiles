#!/usr/bin/env bash

set -euo pipefail

export CWD="${CWD:-$(pwd)}"

require_cmd() {
  command -v "$1" >/dev/null 2>&1 || die "missing required command: $1"
}

main() {
  require_cmd glow
  cd "${CWD}" || exit 1
  glow "${@}"
}

main "$@"
