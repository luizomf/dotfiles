#!/usr/bin/env bash

set -euo pipefail

# MODEL=${MODEL:-qwen3.8:27b-mlx}
MODEL=${MODEL:-qwen3.8:27b-mlx}

# ollama launch claude --model "${MODEL}" -- \
#   --strict-mcp-config --setting-sources user \
#   --permission-mode acceptEdits \
#   --allowedTools "WebSearch" "Bash" \
#   --tools "WebSearch" "Bash" \
#   --verbose --no-session-persistence --effort high -p -

ollama launch codex --model "${MODEL}" -- \
  --search --yolo -c model_reasoning_effort=high \
  exec --ephemeral

